import { EmptyView } from "public/component";
import React, { Component } from "react";
import { View, Text } from "react-native";

export default function CustomerGroups(props) {
  return (
    <View style={{ flex: 1 }}>
      <EmptyView />
    </View>
  );
}
